# =============================================================================
# Track sources + runtime/packaging assets needed to build, package, and run.
# =============================================================================

# --- OS / editor junk ---
.DS_Store
**/.DS_Store
Thumbs.db
Desktop.ini
*~
*.swp
*.swo
*.tmp
.idea/
.vscode/
.cursor/
*.user
*.suo
*.code-workspace

# --- Build outputs / caches ---
/build/
/build-*/
/dist/
/.cache/
.cache/
CMakeCache.txt
CMakeFiles/
cmake-build-*/
compile_commands.json
.clangd/
.cmake/

# --- Machine-local secrets / absolute paths ---
/scripts/env.local
.env
.env.local
.env.*.local

# Local tool checkouts dropped inside the repo
/vcpkg/
/VulkanSDK/
/sonolus-sirius-engine/

# Official Unity/IL2CPP dumps (local reference only; not part of the editor)
/wds-code/
/wds-resources/

# MSI Binary-table dumps (not build inputs; WixUI assets come from wixl-data)
/Binary/

# Optional local-only docs
/docs/

# Optional sample BGM for local testing
/suzume_no_tojimari.ogg
# Local Qt migration verification outputs
build-*.log
all-tests*.log
*-build.log
*-config.log
*-tests.log
install.log
package-test/
package-qt-final/
verify*.log
env-test.log
host-build.log
integration*.log
qt*.log
final-qt.log
audio-test.log
deploy-final.log
